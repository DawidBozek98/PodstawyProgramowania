# Definiujemy funkcję, która zwraca ciąg liczb od 1 do n w formie tekstu

def f(n):
    result = ""  # Zaczynamy z pustym ciągiem 

    for i in range(1, n + 1):  # Pętla przechodzi przez liczby od 1 do n (włącznie)
        result += str(i)  # Dodajemy każdą liczbę do wyniku jako tekst
    return result  # Zwracamy wynik jako ciąg tekstowy

# Testowanie funkcji f
print(f(11))  # Wynik: "1234567891011"
print(f(4))   # Wynik: "1234"